@{
    ViewBag.Title = "Home Page";
}
<div class="jumbotron">
    <div class="text-center" style="height: 100%; padding-top: 20%; padding-bottom: 20%;">
        <h2 class="title">Sistema de Gestão de Talentos</h2>
        @if(ViewBag.Password != null)
            {
                <h3>Caso a senha não chegue no e-mail, @ViewBag.Password</h3>
            }
    </div>
</div>

